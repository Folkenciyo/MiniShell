#include "minishell.h"

void    cmd_create(t_data *data)
{
    t_cmd   *new;
    t_token *token;
    int		fd_in;
	int		status;

	fd_in = 0;
	token = data->token_list;
	while (token && !status)
	{
		
	}
	
}